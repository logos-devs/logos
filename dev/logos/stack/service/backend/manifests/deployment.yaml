apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-deployment
  labels:
    app: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: backend-image:latest
          imagePullPolicy: Always
          env:
            - name: STORAGE_PG_BACKEND_JDBC_URL
              value: jdbc:postgresql://db-rw.logos.dev/logos
            - name: STORAGE_PG_BACKEND_USER
              value: backend
#            - name: SERVER_AUTH_PRIVATE_KEY
#              valueFrom:
#                secretKeyRef:
#                  name: server-auth-key
#                  key: private-key
#                  optional: false
          ports:
            - containerPort: 8081
