apiVersion: apps/v1
kind: Deployment
metadata:
  name: debug-deployment
spec:
  selector:
    matchLabels:
      app: debug
  template:
    metadata:
      labels:
        app: debug
    spec:
      containers:
        - name: debug
          image: debug-image:latest
          imagePullPolicy: Always
